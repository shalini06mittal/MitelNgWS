import { BlogDirective } from './blog.directive';

describe('BlogDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogDirective();
    expect(directive).toBeTruthy();
  });
});
