import { FlagsDirective } from './flags.directive';

describe('FlagsDirective', () => {
  it('should create an instance', () => {
    const directive = new FlagsDirective();
    expect(directive).toBeTruthy();
  });
});
